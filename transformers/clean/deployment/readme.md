This folder holds transformer model configs, for training and inference.

The committed configs may or may not be up to date; the golden standard will
only be be `default_config.jsonc`, use/copy it by default.